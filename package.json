{
  "name": "private-dot-dns-server",
  "version": "1.0.0",
  "description": "A private DNS-over-TLS resolver using Google's DoT",
  "main": "node.js",
  "scripts": {
    "start": "node.js"
  },
  "dependencies": {
    "dns2": "^2.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}
