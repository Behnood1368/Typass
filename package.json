{
  "name": "dns-over-tls-api",
  "version": "1.0.0",
  "description": "HTTP-based DNS-over-TLS resolver using Google DNS",
  "main": "node.js",
  "scripts": {
    "start": "node node.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "tls": "^0.0.1",
    "dns-packet": "^5.6.4"
  },
  "engines": {
    "node": ">=16"
  }
}
